
// using XNodeEditor;

namespace Plugins.Reflexity.Utils.TagList {
    public static class TagListHelper {
    
        public static string[] GetHistoricTags() {
            // if (NodeEditorWindow.current && NodeEditorWindow.current.graph is AIBrainGraph aiBrainGraph) {
                // return aiBrainGraph.HistoricTags.ToArray();
            // }
            return new string[0];
        }
        
        public static string[] GetMemoryTags() {
            // if (NodeEditorWindow.current && NodeEditorWindow.current.graph is AIBrainGraph aiBrainGraph) {
                // return aiBrainGraph.MemoryTags.ToArray();
            // }
            return new string[0];
        }
        
    }
}